

import numpy as np

relevant_directions = {0,3,1}
for idirection,direction in enumerate(relevant_directions):
    print(idirection,direction)

print('---------------')
relevant_directions = [0,3,1]
for idirection,direction in enumerate(relevant_directions):
    print(idirection,direction)
